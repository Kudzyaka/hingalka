services:
  web:
    build: .
    container_name: hingalka_web
    ports:
      - "127.0.0.1:8080:80"
    restart: always

  bot:
    build: ./bot
    container_name: hingalka_bot
    restart: always
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
